# Views Placeholder

This folder is intentionally left without page `.vue` implementations for now.

Planned pages can later mirror RMzyme naming (e.g. `HomePage.vue`, `Search.vue`, `upload.vue`) or use AREK-specific names.

